build:
  ms-messages:
    context: .

deploy:
  - helm upgrade --install ms-messages chart
    --set messages.image=${OKTETO_BUILD_CONTACTS_SERVICE_IMAGE}

dev:
  ms-messages:
    command: npm start
    sync:
      - .:/app